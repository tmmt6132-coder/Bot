# Telegram Bot Project

ဒီ Project က Telegram Bot တစ်ခု ဖြစ်ပါတယ်။

## ပါဝင်သော နာမည်ကြီး Library များ
* pyTelegramBotAPI
* OpenCV
* ddddocr

## စတင်အသုံးပြုပုံ
1. `requirements.txt` ထဲက library များကို install လုပ်ပါ:
   `pip install -r requirements.txt`
2. `Bot.py` ကို run ပါ:
   `python Bot.py`
